# Frontend Agent Improvement Log — 2026-04-05

## Changes Made

### 1. ConfidenceDot touch target (WCAG 2.2 fix)
- **File**: `src/components/confidence-dot.tsx`
- **Before**: 10x10px dot, fails 24px minimum touch target
- **After**: 24x24px invisible wrapper button around the 10px dot

### 2. Focus ring opacity (WCAG 2.2 criterion 2.4.13)
- **File**: `src/app/globals.css`
- **Before**: `outline-primary/50` (50% opacity may fail contrast)
- **After**: `outline-primary` (full opacity)

### 3. LCP priority on first 2 DishCard images
- **File**: `src/components/dish-card.tsx`, `src/app/page.tsx`
- **Before**: No `priority` prop on any DishCard image
- **After**: `isPriority` prop added; first 2 grid items get `priority={true}` on their `<Image>`

### 4. Dark mode food photo brightness
- **File**: `src/components/dish-card.tsx`
- **Before**: Food photos oversaturated on dark backgrounds
- **After**: Added `dark:brightness-90` to dish card images

### 5. Search debounce fix
- **File**: `src/app/page.tsx`
- **Before**: `search.q` updated immediately on every keystroke, triggering fetch via useEffect; debounce only applied to offset reset
- **After**: Separate `searchText` state for controlled input; `search.q` only updated after 300ms debounce, preventing unnecessary API calls

### 6. Profile link aria-label
- **File**: `src/app/page.tsx`
- **Before**: Profile link had no accessible name
- **After**: Added `aria-label="Profile"`

### 7. Safe-area bottom padding on main content
- **File**: `src/app/page.tsx`
- **Before**: Fixed `pb-16` without safe-area consideration
- **After**: Added `safe-area-bottom` class for iPhone notch support

## Validation
- `npx tsc --noEmit` — passed with 0 errors
