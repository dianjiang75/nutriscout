# API Agent Report — 2026-04-01

## Existing Infrastructure (Already Present)
- Rate limiting via Redis sorted sets (`src/lib/middleware/rate-limiter.ts`) — already applied to search, crawl, feedback routes
- Zod validation for search params (`src/lib/validation/search.ts`)
- Standardized API response helpers (`src/lib/utils/api-response.ts`) — `apiSuccess`, `apiError`, `apiBadRequest`, etc.
- Health check endpoint with parallel service checks
- Retry logic via `fetchWithRetry` utility

## Changes Made

### 1. Crawl endpoint — Zod validation + standardized responses (`src/app/api/crawl/restaurant/route.ts`)
- **Before**: Manual `if (!google_place_id)` check, inconsistent error shape
- **After**: Zod schema validates body, JSON parse errors caught, uses `apiSuccess`/`apiError` helpers
- Also added `jobId` for BullMQ deduplication

### 2. Dish detail — UUID validation + standardized responses (`src/app/api/dishes/[id]/route.ts`)
- **Before**: Any string accepted as dish ID — Prisma throws on invalid UUIDs
- **After**: UUID regex check before DB query, uses `apiBadRequest`/`apiNotFound`/`apiError`

### 3. Similar dishes — Zod validation + standardized responses (`src/app/api/dishes/[id]/similar/route.ts`)
- **Before**: `parseFloat`/`parseInt` on raw params, no validation, leaked error messages to client
- **After**: Zod schema validates lat/lng/radius/limit, UUID check on dish ID

### 4. Feedback — Zod validation + standardized responses (`src/app/api/feedback/route.ts`)
- **Before**: Manual type checking, inconsistent error shape
- **After**: Full Zod schema with UUID validation on dish_id/user_id, enum validation for feedback_type

### 5. Restaurant detail — UUID validation + standardized responses (`src/app/api/restaurants/[id]/route.ts`)
- **Before**: Raw ID passed to Prisma, generic error shapes
- **After**: UUID regex check, consistent error responses

## Validation
- `npx tsc --noEmit`: PASS (0 errors)

## Still Needs Attention
- Auth middleware: no JWT/session checks on write endpoints (crawl, feedback) — anyone can submit
- CORS configuration: no explicit CORS headers set
- Bot detection: no middleware for user-agent filtering
- Missing rate limiting on dish detail, restaurant detail, similar dishes routes
- Search route error response still uses raw `Response.json` instead of `apiError`
