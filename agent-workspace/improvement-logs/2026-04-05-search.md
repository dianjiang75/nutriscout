# Search Agent Report — 2026-04-05

## Audit Results

All 8 known issues from the task list have been verified as **already fixed** by prior sessions (primarily 2026-04-04 search agent run):

| # | Issue | Status | Where Fixed |
|---|-------|--------|-------------|
| 1 | Distance filtering missing | Fixed | `geo.ts` earthdistance + orchestrator `distanceMap` |
| 2 | Pagination + caching broken | Fixed | Cache stores full result set; paginated slices from cache |
| 3 | wait_time sort fallback | Fixed | In-memory sort on `estimated_wait_minutes`, Infinity for nulls |
| 4 | macro_match sort not implemented | Fixed | `macroMatchScore()` with per-goal scoring including GLP-1 |
| 5 | Similarity hard-coded normalization | Fixed | Z-score standardization via `MACRO_STATS` |
| 6 | Similarity ignores geo radius | Fixed | Vector path uses earthdistance, JS fallback uses haversine |
| 7 | No full-text search | Fixed | `websearch_to_tsquery()` English + simple fallback |
| 8 | Category filtering | Fixed | `CUISINE_IDS` set for cuisine, direct `category` for meals |

## Additional Verification

- `npx tsc --noEmit` — clean (0 errors)
- Geo duplicate query optimization confirmed (geoResultsCache reuse at step 3)
- Restaurant diversity cap (3/restaurant, 6/cuisine) with re-sort in all modes
- Apollo Evaluator: allergy-critical thresholds, EU Big 14 allergens, keyword safety nets
- Cache key includes searchText, categories, sortBy, allergens, maxWaitMinutes

## No Changes Made

All issues were already resolved. No code modifications needed this session.

## Future Opportunities (not bugs — new features)

- Hybrid RRF search (FTS + vector via Reciprocal Rank Fusion)
- Query intent classification (regex-based routing)
- Dietary safety cache partitioning (separate cache by dietary hash)
- Price perception field in search results
- Per-cuisine diversity cap tuning based on query breadth
